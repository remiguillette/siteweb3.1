version: 1.0

runtime: nodejs18

build:
  commands:
    pre-build:
      - npm install
    build:
      - npm run build

run:
  command: npm run start
  network:
    port: 8080

  env:
    LOG_DIRECTORY: /tmp
    NODE_ENV: production